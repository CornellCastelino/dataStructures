#include<stdio.h>
typedef struct num num;
struct num{
    int data;
    *node = NULL;
}
int main(){
    int size, n, x;
    int num[]
    num* head= (num*)malloc( sizeof(Node) );
    
    printf("Enter number of elements to sum:"):
    scanf("%d",size);

    printf("Enter number "):
    scanf("%d",size);

    head->data =  n;
    for (int i = 0; i < size;i++){

    }

    return 0;
}